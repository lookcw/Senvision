*Here are directories that contain Tweets.
*filteredTweets contains the Tweets that we do all our work on as they are already processed.
*within filteredTweets is a directory for each company. Within each of those directories are files containing Tweets for each day for that respective company.
*We stream Tweets that have the company name, or its ticker name (e.g., Intel = INTC), in them.
